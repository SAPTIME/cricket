# TODO: доработки после MVP

## Спринт 3–4
- [ ] Кнопка «Отметить как выученное» в режиме обучения 
  (вызывает PATCH /cards/:id/status без выхода из сессии).

## Спринт 5–6
- [ ] UI для привязки TOTP-секрета администратора (QR-код).
- [ ] Системный cron для `ensure_future_partitions` (в проде).

## После MVP (Волна 2)
- [ ] API восстановления из `deleted_cards` / `deleted_users`.
- [ ] Фоновые воркеры (Celery / ARQ) — email, очистка протухших сессий, 
  purge `deleted_users` после `purge_after`.
- [ ] Экспорт данных пользователя.

## Оптимизация при росте
- [ ] При > 5000 пользователей: убрать триггер `updated_at` 
  с `card_stats`, обновлять в приложении.
- [ ] При > 10 000: вынос `events` в ClickHouse.